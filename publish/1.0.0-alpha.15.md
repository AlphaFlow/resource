Add new `Resource.refresh` method for use within mutations.

Remove unused `dataStore.activeMutation` information.

Prevent yield writes to resources with failed `get`s. Add error logs for failed resource `get`s.
